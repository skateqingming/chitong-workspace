# 本地开发
NODE_ENV=development
HOST=127.0.0.1
PORT=3000
DATA_FILE=data/app.json
MAX_BODY_BYTES=1048576

# 正式上线后填写 PostgreSQL 连接串（腾讯云 PostgreSQL、阿里云 RDS 或自建 PostgreSQL 都可以）。
# 当前代码仍使用 JSON 文件作为演示存储；DATABASE_URL 先作为部署预留配置。
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require
